# Audio

Placeholder SFX are generated procedurally by `tools/GenSfx.tscn`
(run once: `godot --path . res://tools/GenSfx.tscn`, then re-import).

Replace these `.wav` files with real audio when it's ready — `AudioManager`
loads them by name, so no code changes are needed.
